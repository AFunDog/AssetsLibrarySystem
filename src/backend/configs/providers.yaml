# 提供商配置
# api_key 留空时会回退读取环境变量，如 DASHSCOPE_API_KEY

asset_describer:
  provider: dashscope
  model: qwen-vl-max
  api_key: ""
  temperature: 0.2
  max_tokens: 1024
  reasoning_effort: null
  extra_body: {}
